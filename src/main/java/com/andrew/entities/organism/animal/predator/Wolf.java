package com.andrew.entities.organism.animal.predator;

public class Wolf extends Predator{

    public Wolf(int weight, int saturation, int maxSpeed) {
        super(weight, saturation, maxSpeed);
    }
}
